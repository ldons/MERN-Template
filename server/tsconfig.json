{
  "compilerOptions": {
    /* Language and Environment */
    "target": "ES2016",                                /* Target modern JavaScript for better performance. */
    "module": "CommonJS",                              /* Use CommonJS for Node.js compatibility. */
    "lib": ["ES2021"],                                 /* Include ES2021 standard library declarations. */
    "allowSyntheticDefaultImports": true,             /* Allow default imports for compatibility with CommonJS. */
    "esModuleInterop": true,                           /* Enable interoperability between CommonJS and ES modules. */

    /* Directories */
    "rootDir": "./src",                                /* Specify the root folder containing TypeScript files. */
    "outDir": "./dist",                                /* Specify the output folder for compiled JavaScript files. */
    "baseUrl": "./",                                   /* Base directory for resolving non-relative imports. */
    "paths": {                                         /* Path mapping for cleaner imports. */
      "@/*": ["src/*"]
    },

    /* Type Checking */
    "strict": true,                                    /* Enable all strict type-checking options. */
    "forceConsistentCasingInFileNames": true,          /* Ensure consistent file name casing in imports. */
    "skipLibCheck": true,                              /* Skip type checks for library declaration files. */

    /* Node.js Specific */
    "moduleResolution": "node",                       /* Use Node.js-style module resolution. */
    "resolveJsonModule": true,                        /* Enable importing .json files. */
    "types": ["node"],                                /* Include type declarations for Node.js. */

    /* Output Options */
    "sourceMap": true,                                /* Generate source maps for debugging. */
    "removeComments": true,                           /* Remove comments in the output files. */

    /* Experimental Features */
    "experimentalDecorators": true,                   /* Enable experimental support for decorators (optional). */
    "emitDecoratorMetadata": true                     /* Emit metadata for decorators (optional). */
  },
  "include": ["src/**/*"],                             /* Include all files in the src folder. */
  "exclude": ["node_modules", "dist"]                 /* Exclude unnecessary folders. */
}
